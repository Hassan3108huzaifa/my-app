import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['lh3.googleusercontent.com', 'avatars.githubusercontent.com', 'source.unsplash.com', 'images.pexels.com']
  }
};

export default nextConfig;
